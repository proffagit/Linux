# list content in paths sorted by file size.
du -sh ~/.* ~/* | sort -h
#
du -sh ~/snap/* | sort -h

# list drives
df -h

# list proecessor count
nproc

# remove old fingerprint 
ssh-keygen -f '/home/proffa/.ssh/known_hosts' -R '179.155.187.238'
